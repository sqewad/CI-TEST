language: python
python:
  - "3.6"
# command to install dependencies
install: 
  - pip install coveralls
# command to run tests
script:
  coverage run --source=Guests test.py test
after_success:
  coveralls
